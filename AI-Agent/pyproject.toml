[project]
name = "ai-agent"
version = "0.1.0"
description = "Agent conversationnel Mistral pour l'immobilier au Maroc"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-mistralai>=0.2.0",
    "python-dotenv>=1.2.2",
]
